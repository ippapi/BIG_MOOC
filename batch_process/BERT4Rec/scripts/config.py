from types import SimpleNamespace
args = SimpleNamespace(
    train_batch_size=128,
    val_batch_size=128,
    test_batch_size=128,
    train_negative_sample_size=0,
    train_negative_sampling_seed=0,
    test_negative_sample_size=100,
    test_negative_sampling_seed=98765,
    is_distributed=True,
    device='cpu',
    optimizer='Adam',
    lr=0.001,
    enable_lr_schedule=True,
    weight_decay=0.00,
    decay_step=25,
    gamma=1.0,
    num_epochs=1,
    evaluate_every=1,
    metric_ks=[1, 5, 10],
    best_metric='Recall@10',
    stopping_steps=2,
    bert_dropout=0.1,
    bert_hidden_units=256,
    bert_mask_prob=0.15,
    bert_max_len=15,
    bert_num_blocks=2,
    bert_num_heads=4,
    data_dir='./data',
    save_dir='./logs',
    dataloader_random_seed=2025,
    pretrained_course_embedding_path='./data/course_embeddings.pt'
)
